#include <iostream>
#include <set>
using namespace std;
int main(){
    set<int> st;
    
    int c;
    cin>>c;
    for (int i=0; i<c; i++){
        int m;
        cin>>m;
        st.insert(m);
        }
    int pr = 1;
    pr=pr**st.rbegin();
    pr=pr*(*st.rbegin()-1);
    cout<<pr;
}
